using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RegimentPath : MonoBehaviour
{
    public List<Vector3> regimentPathList = new List<Vector3>();
    // Start is called before the first frame update


    void GetToNextRoundPosition()
    {
        //activate a bool in combat scripts saying this function is working do not get to the next turn ==> let the player set the speed of the character
        // get the moves per round data = x 
        //get the first x values of pathlist and store it in a list pathtogo
        //foreach values of pathtogo : move to 

    }
}
